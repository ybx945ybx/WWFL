<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:tools="http://schemas.android.com/tools"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="@drawable/bg_item_selectable_bordless"
                android:gravity="center|bottom"
                android:orientation="vertical">

    <!--                android:background="@drawable/bg_item_selectable_bordless"
-->

    <TextView
        android:id="@+id/tab_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="@color/sl_tablayout_color"
        android:textSize="@dimen/text_size_medium"
        tools:text="最新优惠" />

    <View
        android:id="@+id/viewIndicator"
        android:layout_width="match_parent"
        android:layout_height="2dp"
        android:layout_alignLeft="@+id/tab_text"
        android:layout_alignRight="@+id/tab_text"
        android:layout_below="@+id/tab_text"
        android:layout_marginTop="6dp"
        android:background="@color/orangeFF804D" />

    <!--android:background="@drawable/bg_tab_indicator"-->
</RelativeLayout>