<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              xmlns:app="http://schemas.android.com/apk/res-auto"
              android:layout_width="match_parent"
              android:layout_height="match_parent"
              android:background="@color/white"
              android:orientation="vertical">

    <!--<include layout="@layout/ht_layout_common_header" />-->

    <!-- 标题栏 -->
    <com.haitao.view.HtHeadView
        android:id="@+id/head_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:center_text="@string/feedback"
        app:right_text="@string/submit"
        app:right_text_color="@color/sl_feedback_submit"
        app:right_text_enabled="false" />


    <EditText
        android:id="@+id/et_content"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@null"
        android:gravity="top"
        android:hint="@string/feedback_hint"
        android:paddingBottom="@dimen/px35"
        android:paddingLeft="@dimen/px24"
        android:paddingRight="@dimen/px24"
        android:paddingTop="@dimen/px35"
        android:textColor="@color/midGrey2"
        android:textColorHint="@color/grey"
        android:textSize="@dimen/text_size_medium" />
</LinearLayout>
